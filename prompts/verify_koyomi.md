# Task

前回の暦表抽出結果に検証エラーが見つかりました。
エラーを修正して正しい JSON を再出力してください。

# 前回の抽出結果

```json
{PREVIOUS_JSON}
```

# 検証エラー

{VALIDATION_ERRORS}

# ドメイン知識（再確認）

## 干支60組（循環順序）
{KANSHI_LIST}

## 元号
{GENGOU_LIST}

## 修正指針

1. 干支の順序エラー → 60組の循環テーブルを見て正しい干支に修正
2. 日数エラー → その月の実際の日数（28〜31）に合わせる
3. 元号エラー → 元号一覧を参照して正しい表記に修正
4. "[?]" が多すぎる → 画像の該当箇所を再確認して可能な限り特定する

# 出力フォーマット

修正済みの完全な JSON のみを出力してください。説明文は不要です。
